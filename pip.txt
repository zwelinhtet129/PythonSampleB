pip													
pip --version           						=version check	
pip install										=pip installation
python -m pip install --upgrade  --upgrade pip	=pip upgrade
--user											=user permission
pip install numpy								=numpy install
pip install -r requirements.txt